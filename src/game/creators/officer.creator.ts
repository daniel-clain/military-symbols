/* import { Officer_A, Officer } from "../types/officer.type"

export const createOfficer = (args: Officer_A): Officer => {

  const leader: Officer = {
    name: args.name!, 
    rank: args.rank!,
    inCommandOf: args.inCommandOf!
  }
  
  return leader

} */